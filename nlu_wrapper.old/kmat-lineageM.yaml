---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: kmat-lineagem
  labels:
    app: kmat-lineagem
    domain: economy
spec:
  selector:
    matchLabels:
      app: kmat-lineagem
  replicas: 1
  template:
    metadata:
      labels:
        app: kmat-lineagem
    spec:
      restartPolicy: Always
      imagePullSecrets:
        - name: registry
      containers:
        - name: default
          image: registry.nlp-utils/kmat:latest
          env:
            - name: PORT_NUM
              value: "80"
            - name: DOMAIN
              value: "lineageM"
          ports:
            - containerPort: 80

---
apiVersion: v1
kind: Service
metadata:
  name: kmat-lineagem
  labels:
    app: kmat-lineagem
spec:
  type: ClusterIP
  clusterIP: None
  selector:
    app: kmat-lineagem
  ports:
    - port: 80
      targetPort: 80

