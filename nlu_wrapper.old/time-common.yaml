---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: time-common
  labels:
    app: time-common
    domain: common
spec:
  selector:
    matchLabels:
      app: time-common
  replicas: 1
  template:
    metadata:
      labels:
        app: time-common
    spec:
      restartPolicy: Always
      imagePullSecrets:
        - name: registry
      containers:
        - name: default
          image: registry.nlp-utils/nctime-2018:latest
          env:
            - name: PORT_NUM
              value: "80"
          ports:
            - containerPort: 80

---
apiVersion: v1
kind: Service
metadata:
  name: time-common
  labels:
    app: time-common
spec:
  type: ClusterIP
  clusterIP: None
  selector:
    app: time-common
  ports:
    - port: 80
      targetPort: 80

