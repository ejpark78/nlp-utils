---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: parser-common
  namespace: crawler
  labels:
    app: parser-common
    domain: common
spec:
  selector:
    matchLabels:
      app: parser-common
  replicas: 1
  template:
    metadata:
      labels:
        app: parser-common
    spec:
      restartPolicy: Always
      containers:
        - name: default
          image: galadriel02.korea.ncsoft.corp:5000/dependency-parser:latest
          env:
            - name: PORT_NUM
              value: "80"
          ports:
            - containerPort: 80

---
apiVersion: v1
kind: Service
metadata:
  name: parser-common
  namespace: crawler
  labels:
    app: parser-common
spec:
  type: LoadBalancer
  selector:
    app: parser-common
  externalIPs:
    - 172.20.78.250
  ports:
    - port: 30040
      targetPort: 80

