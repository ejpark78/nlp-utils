
---
apiVersion: batch/v1beta1
kind: CronJob
metadata:
  name: game-info
  namespace: crawler-naver-kbo-game-center
  labels:
    app: crawler-naver-kbo-game-center
spec:
  schedule: "* 18,19,20,21,22,23,24 * * *"
  concurrencyPolicy: Forbid
  failedJobsHistoryLimit: 3
  successfulJobsHistoryLimit: 3
  jobTemplate:
    spec:
      template:
        spec:
          restartPolicy: OnFailure
          containers:
            - name: default
              image: corpus:5000/crawler:latest
              imagePullPolicy: Always
              workingDir: /usr/local/app
              args: ["python3", "module/naver/kbo_game_center_utils.py", "--game_info"]

---
apiVersion: batch/v1beta1
kind: CronJob
metadata:
  name: trace-comments
  namespace: crawler-naver-kbo-game-center
  labels:
    app: crawler-naver-kbo-game-center
spec:
  schedule: "* 18,19,20,21,22,23,24 * * *"
  concurrencyPolicy: Forbid
  failedJobsHistoryLimit: 3
  successfulJobsHistoryLimit: 3
  jobTemplate:
    spec:
      template:
        spec:
          restartPolicy: OnFailure
          containers:
            - name: default
              image: corpus:5000/crawler:latest
              imagePullPolicy: Always
              workingDir: /usr/local/app
              args: ["python3", "module/naver/kbo_game_center_utils.py", "--trace_comments"]
