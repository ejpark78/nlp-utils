version: '3'

services:

  mlbpark-kbo:
    image: corpus:5000/crawler:2.0
    restart: always
    working_dir: /usr/local/app
    command: python3 batch.py -category bbs -job_id mlbpark-kbo
    extra_hosts:
      - "g7:172.20.78.254"
      - "corpus.ncsoft.com:172.20.79.241"
    volumes:
      - /etc/timezone:/etc/timezone:ro
      - /etc/localtime:/etc/localtime:ro
      - $PWD/../status:/usr/local/app/status:rw

  mlbpark-mlb:
    image: corpus:5000/crawler:2.0
    restart: always
    working_dir: /usr/local/app
    command: python3 batch.py -category bbs -job_id mlbpark-mlb
    extra_hosts:
      - "g7:172.20.78.254"
      - "corpus.ncsoft.com:172.20.79.241"
    volumes:
      - /etc/timezone:/etc/timezone:ro
      - /etc/localtime:/etc/localtime:ro
      - $PWD/../status:/usr/local/app/status:rw

  mlbpark-bullpen:
    image: corpus:5000/crawler:2.0
    restart: always
    working_dir: /usr/local/app
    command: python3 batch.py -category bbs -job_id mlbpark-bullpen
    extra_hosts:
      - "g7:172.20.78.254"
      - "corpus.ncsoft.com:172.20.79.241"
    volumes:
      - /etc/timezone:/etc/timezone:ro
      - /etc/localtime:/etc/localtime:ro
      - $PWD/../status:/usr/local/app/status:rw

  lineagem:
    image: corpus:5000/crawler:2.0
    restart: always
    working_dir: /usr/local/app
    command: python3 batch.py -category bbs -job_id lineagem
    extra_hosts:
      - "g7:172.20.78.254"
      - "corpus.ncsoft.com:172.20.79.241"
    volumes:
      - /etc/timezone:/etc/timezone:ro
      - /etc/localtime:/etc/localtime:ro
      - $PWD/../status:/usr/local/app/status:rw

# 네트워크 인터페이스 정의
networks:
  default:
    ipam:
      config:
        - subnet: 190.113.0.0/24
