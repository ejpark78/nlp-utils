version: '2'

services:

  economy:
    image: ${IMAGE}
    restart: always
    working_dir: /usr/local/app
    command: python3 batch.py ${CRAWLER_OPT} -category nate -job_id economy
    extra_hosts:
      - "${MQ_HOSTS}"
      - "elasticsearch:172.20.79.241"
    volumes:
      - /etc/timezone:/etc/timezone:ro
      - /etc/localtime:/etc/localtime:ro
      - $PWD/../status:/usr/local/app/status:rw

  international:
    image: ${IMAGE}
    restart: always
    working_dir: /usr/local/app
    command: python3 batch.py ${CRAWLER_OPT} -category nate -job_id international
    extra_hosts:
      - "${MQ_HOSTS}"
      - "elasticsearch:172.20.79.241"
    volumes:
      - /etc/timezone:/etc/timezone:ro
      - /etc/localtime:/etc/localtime:ro
      - $PWD/../status:/usr/local/app/status:rw

  entertainment:
    image: ${IMAGE}
    restart: always
    working_dir: /usr/local/app
    command: python3 batch.py ${CRAWLER_OPT} -category nate -job_id entertainment
    extra_hosts:
      - "${MQ_HOSTS}"
      - "elasticsearch:172.20.79.241"
    volumes:
      - /etc/timezone:/etc/timezone:ro
      - /etc/localtime:/etc/localtime:ro
      - $PWD/../status:/usr/local/app/status:rw

  it:
    image: ${IMAGE}
    restart: always
    working_dir: /usr/local/app
    command: python3 batch.py ${CRAWLER_OPT} -category nate -job_id it
    extra_hosts:
      - "${MQ_HOSTS}"
      - "elasticsearch:172.20.79.241"
    volumes:
      - /etc/timezone:/etc/timezone:ro
      - /etc/localtime:/etc/localtime:ro
      - $PWD/../status:/usr/local/app/status:rw

  politics:
    image: ${IMAGE}
    restart: always
    working_dir: /usr/local/app
    command: python3 batch.py ${CRAWLER_OPT} -category nate -job_id politics
    extra_hosts:
      - "${MQ_HOSTS}"
      - "elasticsearch:172.20.79.241"
    volumes:
      - /etc/timezone:/etc/timezone:ro
      - /etc/localtime:/etc/localtime:ro
      - $PWD/../status:/usr/local/app/status:rw

  society:
    image: ${IMAGE}
    restart: always
    working_dir: /usr/local/app
    command: python3 batch.py ${CRAWLER_OPT} -category nate -job_id society
    extra_hosts:
      - "${MQ_HOSTS}"
      - "elasticsearch:172.20.79.241"
    volumes:
      - /etc/timezone:/etc/timezone:ro
      - /etc/localtime:/etc/localtime:ro
      - $PWD/../status:/usr/local/app/status:rw

  sports:
    image: ${IMAGE}
    restart: always
    working_dir: /usr/local/app
    command: python3 batch.py ${CRAWLER_OPT} -category nate -job_id sports
    extra_hosts:
      - "${MQ_HOSTS}"
      - "elasticsearch:172.20.79.241"
    volumes:
      - /etc/timezone:/etc/timezone:ro
      - /etc/localtime:/etc/localtime:ro
      - $PWD/../status:/usr/local/app/status:rw

#  tv:
#    image: ${IMAGE}
#    restart: always
#    working_dir: /usr/local/app
#    command: python3 batch.py ${CRAWLER_OPT} -category nate -job_id tv
#    extra_hosts:
#      - "${MQ_HOSTS}"
#      - "elasticsearch:172.20.79.241"
#    volumes:
#      - /etc/timezone:/etc/timezone:ro
#      - /etc/localtime:/etc/localtime:ro
#      - $PWD/../status:/usr/local/app/status:rw

#  opinion:
#    image: ${IMAGE}
#    restart: always
#    working_dir: /usr/local/app
#    command: python3 batch.py ${CRAWLER_OPT} -category nate -job_id opinion
#    extra_hosts:
#      - "${MQ_HOSTS}"
#      - "elasticsearch:172.20.79.241"
#    volumes:
#      - /etc/timezone:/etc/timezone:ro
#      - /etc/localtime:/etc/localtime:ro
#      - $PWD/../status:/usr/local/app/status:rw

# 네트워크 인터페이스 정의
networks:
  default:
    ipam:
      config:
        - subnet: 190.123.0.0/24
