version: '2'

services:

  economy:
    image: ${IMAGE}
    restart: always
    working_dir: /usr/local/app
    command: python3 batch.py -category naver -job_id economy
    extra_hosts:
      - "mq-server:172.20.78.254"
      - "corpus.ncsoft.com:172.20.79.241"
    volumes:
      - /etc/timezone:/etc/timezone:ro
      - /etc/localtime:/etc/localtime:ro
      - $PWD/../status:/usr/local/app/status:rw

  international:
    image: ${IMAGE}
    restart: always
    working_dir: /usr/local/app
    command: python3 batch.py -category naver -job_id international
    extra_hosts:
      - "mq-server:172.20.78.254"
      - "corpus.ncsoft.com:172.20.79.241"
    volumes:
      - /etc/timezone:/etc/timezone:ro
      - /etc/localtime:/etc/localtime:ro
      - $PWD/../status:/usr/local/app/status:rw

  it:
    image: ${IMAGE}
    restart: always
    working_dir: /usr/local/app
    command: python3 batch.py -category naver -job_id it
    extra_hosts:
      - "mq-server:172.20.78.254"
      - "corpus.ncsoft.com:172.20.79.241"
    volumes:
      - /etc/timezone:/etc/timezone:ro
      - /etc/localtime:/etc/localtime:ro
      - $PWD/../status:/usr/local/app/status:rw

  living:
    image: ${IMAGE}
    restart: always
    working_dir: /usr/local/app
    command: python3 batch.py -category naver -job_id living
    extra_hosts:
      - "mq-server:172.20.78.254"
      - "corpus.ncsoft.com:172.20.79.241"
    volumes:
      - /etc/timezone:/etc/timezone:ro
      - /etc/localtime:/etc/localtime:ro
      - $PWD/../status:/usr/local/app/status:rw

  opinion:
    image: ${IMAGE}
    restart: always
    working_dir: /usr/local/app
    command: python3 batch.py -category naver -job_id opinion
    extra_hosts:
      - "mq-server:172.20.78.254"
      - "corpus.ncsoft.com:172.20.79.241"
    volumes:
      - /etc/timezone:/etc/timezone:ro
      - /etc/localtime:/etc/localtime:ro
      - $PWD/../status:/usr/local/app/status:rw

  politics:
    image: ${IMAGE}
    restart: always
    working_dir: /usr/local/app
    command: python3 batch.py -category naver -job_id politics
    extra_hosts:
      - "mq-server:172.20.78.254"
      - "corpus.ncsoft.com:172.20.79.241"
    volumes:
      - /etc/timezone:/etc/timezone:ro
      - /etc/localtime:/etc/localtime:ro
      - $PWD/../status:/usr/local/app/status:rw

  society:
    image: ${IMAGE}
    restart: always
    working_dir: /usr/local/app
    command: python3 batch.py -category naver -job_id society
    extra_hosts:
      - "mq-server:172.20.78.254"
      - "corpus.ncsoft.com:172.20.79.241"
    volumes:
      - /etc/timezone:/etc/timezone:ro
      - /etc/localtime:/etc/localtime:ro
      - $PWD/../status:/usr/local/app/status:rw

  sports:
    image: ${IMAGE}
    restart: always
    working_dir: /usr/local/app
    command: python3 batch.py -category naver -job_id sports
    extra_hosts:
      - "mq-server:172.20.78.254"
      - "corpus.ncsoft.com:172.20.79.241"
    volumes:
      - /etc/timezone:/etc/timezone:ro
      - /etc/localtime:/etc/localtime:ro
      - $PWD/../status:/usr/local/app/status:rw

#  tv:
#    image: ${IMAGE}
#    restart: always
#    working_dir: /usr/local/app
#    command: python3 batch.py -category naver -job_id tv
#    extra_hosts:
#      - "mq-server:172.20.78.254"
#      - "corpus.ncsoft.com:172.20.79.241"
#    volumes:
#      - /etc/timezone:/etc/timezone:ro
#      - /etc/localtime:/etc/localtime:ro
#      - $PWD/../status:/usr/local/app/status:rw

# 네트워크 인터페이스 정의
networks:
  default:
    ipam:
      config:
        - subnet: 190.122.0.0/24
