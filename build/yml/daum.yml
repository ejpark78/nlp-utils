version: '3'

services:

  culture:
    image: corpus:5000/crawler:2.0
    restart: always
    working_dir: /usr/local/app
    command: python3 batch.py -job_category daum -job_id culture
    volumes:
      - /etc/hosts:/etc/hosts:ro
      - /etc/timezone:/etc/timezone:ro
      - /etc/localtime:/etc/localtime:ro
      - $PWD/../config/status:/usr/local/app/config/status:rw

  economy:
    image: corpus:5000/crawler:2.0
    restart: always
    working_dir: /usr/local/app
    command: python3 batch.py -job_category daum -job_id economy
    volumes:
      - /etc/hosts:/etc/hosts:ro
      - /etc/timezone:/etc/timezone:ro
      - /etc/localtime:/etc/localtime:ro
      - $PWD/../config/status:/usr/local/app/config/status:rw

  international:
    image: corpus:5000/crawler:2.0
    restart: always
    working_dir: /usr/local/app
    command: python3 batch.py -job_category daum -job_id international
    volumes:
      - /etc/hosts:/etc/hosts:ro
      - /etc/timezone:/etc/timezone:ro
      - /etc/localtime:/etc/localtime:ro
      - $PWD/../config/status:/usr/local/app/config/status:rw

  it:
    image: corpus:5000/crawler:2.0
    restart: always
    working_dir: /usr/local/app
    command: python3 batch.py -job_category daum -job_id it
    volumes:
      - /etc/hosts:/etc/hosts:ro
      - /etc/timezone:/etc/timezone:ro
      - /etc/localtime:/etc/localtime:ro
      - $PWD/../config/status:/usr/local/app/config/status:rw

  opinion:
    image: corpus:5000/crawler:2.0
    restart: always
    working_dir: /usr/local/app
    command: python3 batch.py -job_category daum -job_id opinion
    volumes:
      - /etc/hosts:/etc/hosts:ro
      - /etc/timezone:/etc/timezone:ro
      - /etc/localtime:/etc/localtime:ro
      - $PWD/../config/status:/usr/local/app/config/status:rw

  politics:
    image: corpus:5000/crawler:2.0
    restart: always
    working_dir: /usr/local/app
    command: python3 batch.py -job_category daum -job_id politics
    volumes:
      - /etc/hosts:/etc/hosts:ro
      - /etc/timezone:/etc/timezone:ro
      - /etc/localtime:/etc/localtime:ro
      - $PWD/../config/status:/usr/local/app/config/status:rw

  society:
    image: corpus:5000/crawler:2.0
    restart: always
    working_dir: /usr/local/app
    command: python3 batch.py -job_category daum -job_id society
    volumes:
      - /etc/hosts:/etc/hosts:ro
      - /etc/timezone:/etc/timezone:ro
      - /etc/localtime:/etc/localtime:ro
      - $PWD/../config/status:/usr/local/app/config/status:rw

  sports:
    image: corpus:5000/crawler:2.0
    restart: always
    working_dir: /usr/local/app
    command: python3 batch.py -job_category daum -job_id sports
    volumes:
      - /etc/hosts:/etc/hosts:ro
      - /etc/timezone:/etc/timezone:ro
      - /etc/localtime:/etc/localtime:ro
      - $PWD/../config/status:/usr/local/app/config/status:rw

# 네트워크 인터페이스 정의
networks:
  default:
    ipam:
      config:
        - subnet: 190.126.0.0/24
