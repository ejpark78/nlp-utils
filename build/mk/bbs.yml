version: '2'

services:

  mlbpark-kbo:
    image: "${CRAWLER_IMAGE}"
    restart: "${RESTART}"
    working_dir: /usr/local/app
    command: python3 batch.py ${CRAWLER_OPT} -category bbs -job_id mlbpark-kbo
    extra_hosts:
      - "${CORPUS_HOST_NAME}"
    volumes:
      - $PWD/../status:/usr/local/app/status:rw

  mlbpark-mlb:
    image: "${CRAWLER_IMAGE}"
    restart: "${RESTART}"
    working_dir: /usr/local/app
    command: python3 batch.py ${CRAWLER_OPT} -category bbs -job_id mlbpark-mlb
    extra_hosts:
      - "${CORPUS_HOST_NAME}"
    volumes:
      - $PWD/../status:/usr/local/app/status:rw

  mlbpark-bullpen:
    image: "${CRAWLER_IMAGE}"
    restart: "${RESTART}"
    working_dir: /usr/local/app
    command: python3 batch.py ${CRAWLER_OPT} -category bbs -job_id mlbpark-bullpen
    extra_hosts:
      - "${CORPUS_HOST_NAME}"
    volumes:
      - $PWD/../status:/usr/local/app/status:rw

  lineagem:
    image: "${CRAWLER_IMAGE}"
    restart: "${RESTART}"
    working_dir: /usr/local/app
    command: python3 batch.py ${CRAWLER_OPT} -category bbs -job_id lineagem
    extra_hosts:
      - "${CORPUS_HOST_NAME}"
    volumes:
      - $PWD/../status:/usr/local/app/status:rw

  ruliweb:
    image: "${CRAWLER_IMAGE}"
    restart: "${RESTART}"
    working_dir: /usr/local/app
    command: python3 batch.py ${CRAWLER_OPT} -category bbs -job_id ruliweb
    extra_hosts:
      - "${CORPUS_HOST_NAME}"
    volumes:
      - $PWD/../status:/usr/local/app/status:rw

  inven-lineagem:
    image: "${CRAWLER_IMAGE}"
    restart: "${RESTART}"
    working_dir: /usr/local/app
    command: python3 batch.py ${CRAWLER_OPT} -category bbs -job_id inven-lineagem
    extra_hosts:
      - "${CORPUS_HOST_NAME}"
    volumes:
      - $PWD/../status:/usr/local/app/status:rw
