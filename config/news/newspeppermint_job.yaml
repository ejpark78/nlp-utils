---
reference: config/template_kor.yaml
dag:
  dag_id: 'newspeppermint'
  description: 'NewsPeppermint 크롤러'
  schedule_interval: '15 * * * *'
  tags:
    - NewsPeppermint
operator:
  params:
    resources:
      request_cpu: "200m"
      request_memory: "256Mi"
      limit_cpu: "200m"
      limit_memory: "256Mi"
tasks:
  - category: world
    config: /config/news/newspeppermint.yaml
    name: 세계,한국,경제,경영,IT,과학,문화,스포츠,칼럼,추천
    task_id: all
